# 给 Codex 的执行约束与首轮任务 v4

本文件与 `plan_v4.md`、`TASKS_v4.md` 配套。当前交付只是执行规格，仓库中的分析代码和数据结果需要另行实现。

## 全局约束（新对话开始时提供）

```text
项目：电商增长分析与实验可信性验证。
任务依据：plan_v4.md 和 TASKS_v4.md；旧v3仅用于迁移参考，冲突按v4执行。
目标：面向拼多多数据分析师岗位，优先数据正确性、业务判断、实验设计与可复现证据。

每次只执行一个指定任务或明确子任务。先检查前置产物，不越级生成整套项目。
若规格内部发现歧义，记录具体问题和最小建议；不要静默发明字段、口径或数据来源。

数据规则：
1. raw不可变；金额/日期/分母按metric_contract；只标记候选重复和高频session，不默认删除。
2. SQL负责业务指标和实验表，Python负责调度/统计/绘图；轻量解析与质量标记可在PySpark完成。
3. REES46、Criteo独立，不连接用户；模拟与人工故障只写独立空间。
4. 事实表禁止collect/toPandas；用户表按实际内存预算决定，不采用“500万行以内安全”。
5. 缺金额不当作真实0；已确认完整日志下未购买才是结构性0。
6. 相同输入重复执行不增加指标；不得overwrite整个根目录抹去其他月份。

环境规则：
1. 主环境先本地；MRC权限、配额和用途未经确认不视为可用。
2. 建议Python3.11/JDK17/PySpark与Spark3.5.8作为起点；实际版本锁文件必须来自真实安装。
3. 不无条件brew install最新版Spark/Hadoop；Hadoop在T6独立验证。
4. 不运行破坏性格式化、删除原始文件或修改共享云资源；初始化与启动分开。
5. 密钥、密码、token不写入文件、日志或Git；不发布未授权的数据。

验收规则：
1. 单元测试、独立核验、同种子复现、结果路径与运行记录缺一不可。
2. 不强迫FPR恰好5%、CUPED改善、模型胜出或自然数据出现指定异常。
3. 不调测试集选特征/模型；不挑种子造好看结果。
4. 失败或未运行写failed/not_run；未实测的规模/速度/精度不能填写。
5. 代码注释英文；业务报告中文，关键术语保留英文。

每次结束输出：
- task_id、实际状态、修改文件；
- 命令、版本、配置和输入manifest；
- 输入/输出范围、耗时、run_id；
- 每项验收的expected/actual/pass；
- 未解决问题；
- 3个需要用户能解释的要点；
- 下一项建议，不自动实施。
```

## 今晚第一条指令

```text
请先读取 plan_v4.md 和 TASKS_v4.md。
本轮只执行 T0.1 和 T0.2：
1. 查看已有目录，不覆盖既有文件。
2. 整理项目目标、P0/P1/P2和两份数据的证据边界。
3. 执行只读环境检测，记录操作系统、CPU架构、内存、磁盘、Python/Java/Spark实际版本。
4. 没有MRC有效项目与授权证据时标记unverified，不尝试创建实例或上传数据。
5. 生成对应文档和下一轮T0.3建议；本轮不安装Hadoop、不下载全量数据、不生成虚构分析结果。
输出执行收据与待确认的版本冲突，不开始后续任务。
```

## 后续单任务模板

```text
本轮只执行任务：[填写T编号]。
请先读取plan_v4.md中该任务和依赖产物，检查TASKS状态。
输入范围：[填写真实文件/日期/样本scope]。
允许修改范围：[填写模块]。
需要遵守metric_contract和固定配置；不修改口径来通过测试。
先做最小人工样本和边界测试，再做指定真实数据范围。
按全局收据返回结果；未实际运行的测试标记not_run，不宣称任务完成。
```

## 你本人每轮确认

你需要确认：这个输出回答什么业务问题；指标的一行和分母是什么；哪些数据被排除及影响；为什么相信结果；还有哪些不能证明。

这不是让Codex替你完成后直接背结论，而是让它辅助实施，你能够审查、解释和维护自己的作品。
