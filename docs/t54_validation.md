# T5.2–T5.4 离线A/A验收

2026-09-21，`aa-oct-01`。起点 `1357d205083fe49714ac20bd4cc47610c889e246`。T5.2、T5.3、T5.4均done（工程验收；本人解释未代验）。统计校准实际结果另列，不把误报恰为5%当工程完成条件。

## 固定输入、方法与运行

仅核对T5.1 `cohort-oct-01`完成收据、身份/数值文件字节与SHA；用户级查询只对values Parquet执行一次，读取cohort_key、post_converted、post_purchase_amount及scope/金额质量状态，上限84,166行并要求实际84,165行。未查询identity内原user_id；没有读取事实、用户日、真实CSV、Criteo或11月数据。指纹沿用T5.1文档所列，原队列、membership、历史报告均未写入。

完整入组人数84,165、购买标记合计4,812、Decimal金额3,499,563.53与收据精确相同；金额状态complete_observed，坏值和空金额用户0。金额转为349,956,353个百分之一原单位，逐值float64回转整数完全一致；总浮点和误差3×10⁻¹⁰，所有运行组均值与精确整数和/人数的最大差1.27×10⁻¹⁴，分别低于冻结容限10⁻⁷及10⁻¹⁰。统计方差可用float64，业务总额仍以整数/Decimal核对，原文件未改。

先冻结config/aa.json及统计契约，保存本地副本和代码SHA，再人工测试、再真实执行。ID0001–0300，SHA256前8字节big-endian阈值2**63；只改变分组，转换与金额数组只读。购买率未合并Wald-z，金额Welch-t；公式、状态和适用前提见[契约](statistical_contract.md)。T3.2只作SE公式参照，不调用其正常近似区间处理金额。

## expected / actual / pass

| 检查 | expected | actual | pass |
|---|---|---|---|
| 稳定分配 | 同ID、跨进程、改变顺序均一致；独立DuckDB SHA256实现相同 | 80个人工键全部一致；真实0001重建一致 | 是 |
| 结果不参与分配 | 接口仅键与实验ID；改变人工结果不改分组 | 相同；真实结果数组SHA前后相同 | 是 |
| SRM | 人数99/1触发，41/39不要求强行相等；与独立chi-square公式相同 | 符合；真实300次标记0，均保留 | 是 |
| 购买率公式 | 人工20/100与40/100：差0.2、SE√0.004，单位/方向/区间对应 | 相同；p另用erfc公式核对 | 是 |
| Welch | 人工均值差4、SE√(0.5+7/3)，df/t区间/p与官方ttest_ind一致 | 相同 | 是 |
| 空组/小样本/常量/非法数 | empty_group、insufficient_n、zero_variance、nonfinite_input；不伪造p | 相同；二元稀疏格返回sparse_cells | 是 |
| ratio | sum(X)/sum(Y)，含协方差；单个Y=0保留，总分母0有状态 | 与独立协方差公式一致，zero_denominator正确 | 是 |
| 成对bootstrap | 同索引同时取X/Y，比例X=2Y构造样本始终为2；同seed一致 | 差值恒0；重复结果一致；未改人工输入 | 是 |
| 汇总选择 | SRM显著运行进主表、仅从辅助表排除；正负显著均保留 | 人工主表2/4、辅助1/3；真实两套分母均300 | 是 |
| 真实计划与守恒 | 300ID×2指标，N/购买/整数金额每次守恒 | 600行、全部可计算；失败0、无缺ID | 是 |
| 独立CSV核对 | 人数、金额组均值、方向、p/CI、Wilson和经验分位数一致 | 全部一致；输入与代码运行前后SHA不变 | 是 |

7项人工测试先行，入口以当前代码再次执行。真实金额样本方差公式的独立库对照在人工数据完成；真实CSV再核对存储SE/df对应的t区间/p，组均值则从整数金额独立复算。没有把这个汇总复查冒充第二次独立读取真实用户或重新估计其方差。

0001与0002分配交叉人数为[[21,314,20,769],[21,144,20,938]]，phi=0.004028，仅描述这两个ID的关系，不要求完全正交或推断所有ID独立。没有因结果相关或SRM表现修改分组。

## ratio人工验证结果

预先指定A120人、B96人，6000次PCG64成对重抽样，全部分母有效。delta SE **0.02970186**，bootstrap差值SD **0.02930588**，相对差 **−1.3332%**；30批各200次，批间SD/√30得到bootstrap SE的近似MC标准误 **0.00027256**。两SE之差0.00039598约为1.45个该MC标准误，只是量级对照，不是正式等价检验；delta近似误差与bootstrap Monte Carlo误差不能混称。bootstrap均值0.22345490相对原比率差0.22222222有0.00123268偏移，符合非线性比率可能存在有限样本偏移的限制，不把一次接近推广为任意数据保证。

X/Y成对重抽样及协方差验收完成，T5.3没有以两个均值接口代替原ratio要求。数值见本地ratio_synthetic.json；没有在真实数据上新增第三个业务指标。

## 校准记录及限制

购买率21/300＝7.00%，正10/负11，Wilson [4.62%,10.46%]；金额17/300＝5.67%，正11/负6，Wilson [3.57%,8.89%]。CI覆盖0分别279/300、283/300；所有失败和SRM计数均0。误报目标逐指标为5%；实现核验通过不等于已证明长期误报率精准为5%。保留全部p值、差值和状态，没有筛选显著例子或重跑改善结果。运行后仅将契约中的官方文档URL修正为可访问的稳定入口，冻结副本保留；公式、配置和实现未改。固定0001确实两项正差且p<.05，详见[业务报告](../reports/aa_validation.md)。

## 依赖与资源

现有Python3.11.15、DuckDB1.5.5。仅安装官方PyPI的NumPy2.4.6与SciPy1.17.1 CPython3.11/macOS14 arm64 wheel，无源码编译；实际环境freeze更新锁，原有6个包版本均未改变，pip check无冲突。NumPy wheel SHA256 `4cfe66903cc32a9921a6733d96b19bb6abf310397581bbad89c228f5abaf0ee8`，SciPy `a3472cfbca0a54177d0faa68f697d8ba4c80bbdc19908c3465556d9f7efce9ee`；安装来源/收据留在.local/t54。未升级pip、Spark或修改全局环境。

真实300次阶段13.613秒，入口总14.062秒（含人工测试/小型bootstrap、队列核验和一次读取，不含安装），只作运行记录。新依赖落地133,510,274 bytes；运行结束依赖＋本轮本地输出133,896,308 bytes，远低于1GiB；当时空闲547,370,315,776 bytes，高于150GiB。阶段检查每25次执行；执行进程ru_maxrss峰值241,876,992 bytes（Darwin字节），不包含pip安装或所有子进程。下载临时安装解包峰值未采样，安装载荷约25.8MB，另留该限制，不冒充全任务瞬时峰值。

输出仅600行运行表、4行摘要、人工ratio结果、代码/来源指纹、日志与小型收据；不保存300份成员表，数值数组约1.43MB及键仅在内存。没有启动Spark、线上平台、A/A调度服务，未执行T5.5、CUPED或功效曲线。

```sh
.venv/bin/python -m unittest tests.test_aa -v
# 真实复跑需当轮授权；ID规则仍固定，运行目录不得已存在。
.venv/bin/python abtest/aa_test.py --run-id aa-oct-NEW
```

用户级键与队列保持本地。报告/汇总和代码可提交；raw路径、安装日志及本地运行收据不入Git。无未解决的工程差异；统计外推、金额近似与Monte Carlo误差是使用限制，本人解释未代验。下一轮才考虑T5.5已知效应注入，本轮停止。
