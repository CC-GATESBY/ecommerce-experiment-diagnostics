# POWER-01 验收

2026-09-23，`power-raw-oct-01`：**done（固定队列、未调整购买率功效模拟；本人解释未代验）**。起点`ccd9a389aa49f11d2545956b8f78fcae57b1310a`。规则在本轮输入读取前写入[合同](power_raw_contract.md)和[配置](../config/power_raw.json)，未按结果改网格、种子、轮数或检验。公开仓库维持public/main。

## 输入与运行

- 唯一输入：`cohort-oct-01` / `rees46-pre-oct01-14-post-oct15-28-v1`。成功收据、28天计数质量和窗口匹配。values文件3,168,318 bytes，SHA256 `d361a8660b7e8f4bd40bc4bc5b7a54e2bf4c74343990fcf338c7d22d9a266e97`。
- 仅一次DuckDB查询`cohort_key, post_converted`，实际84,165行、4,812个1，p0精确为1604/28055。原键文件仅检查大小/mtime，不查询user_id。values文件指纹核对读取物理字节，未查询金额列；没有事实、用户日、CSV或Criteo读取。
- 使用现有Python 3.11.15、NumPy 2.4.6、SciPy 1.17.1、DuckDB 1.5.5；未安装/改锁/启动Spark。
- 真实计算只执行一次9×300，不重跑原AA或单次注入；人工跨进程重现检查不读取真实队列。轮次间U和分组指纹留本地；不保存用户数组或2,700份成员表。

人工命令：`.venv/bin/python -m unittest tests.test_power_raw tests.test_inject -v`。

真实入口：`.venv/bin/python scripts/run_power_raw.py --run-id power-raw-oct-01`。该ID已经存在，再次执行应拒绝覆盖，不是重新评估指令。执行日志、事前配置副本及成功收据位于被忽略的`.local/power01/`；键和真实数组不提交。公开仓库只含脱敏逐轮、逐档汇总及代码/文档。

## expected / actual / pass

| 核验 | expected | actual | 结果 |
|---|---|---|---|
| 队列范围 | 84,165行，4,812个购买标记；不按返回筛选 | 一次二列投影完全一致 | pass |
| 人工与必要回归 | 单位、非法delta、p0=1、只0→1、先构造、共享随机源、顺序/跨进程复现、旧接口限制 | 10项新增人工测试及7项原注入测试全部通过 | pass |
| 理论零效应点 | 双侧.05、正向.025 | .049999999999999996 / .024999999999999998 | pass |
| 计划与失败分母 | 9档×300；失败独立保留，SRM不删 | 完成/有效2,700；失败0；300次分配SRM标记0 | pass |
| 逐轮公式独立复算 | 全部行的计数→Fraction、stdlib NormalDist/erfc；Wilson另按显式公式 | 2,700行和9行汇总一致，最大浮点差3.22e−15（概率单位） | pass |
| 同轮潜在结果/分配 | 9档同nA/nB与A购买数；B新增数恒等式；共享U单调潜在结果 | 全轮一致；零效应300轮K=0 | pass |
| 生成机制 | E[K]=N0q、Var[K]=N0q(1−q)；按平均tau的MCSE检查 | 8个正效应点均满足事前\|MC z\|≤6，最大1.331 | pass |
| 80%理论/经验 | 仅固定范围求根；保留全部交叉，不平滑 | 理论0.456707…pp；经验仅0.40→0.46向上跨越；没有扩网格 | pass |
| 不覆盖/不可变 | 已有run拒绝；Y0、cohort和AA/单次注入及历史文件不变 | 人工重复run拒绝；真实Y0字节哈希、输入SHA、旧run大小/mtime与被保护历史文件哈希一致 | pass |

浮点比较使用事前实现的`rel_tol=1e-12, abs_tol=1e-15`；整数和同轮B翻转恒等式精确相等。统计失败/空臂、SRM标记仍进入人工汇总的完成分母，可计算分母排除失败但包含SRM；零次/全拒绝时即使plug-in MCSE=0仍有Wilson区间。真实失败为0不替代这些边界测试。

## 机制与规模

| 效应（pp） | 理论平均K | 实测平均K | 平均tau的MCSE（pp） | MC z |
|---|---:|---:|---:|---:|
| 0.10 | 84.165 | 84.6167 | 0.000629 | 0.853 |
| 0.20 | 168.330 | 169.3233 | 0.000889 | 1.328 |
| 0.30 | 252.495 | 252.3200 | 0.001088 | −0.191 |
| 0.40 | 336.660 | 337.2033 | 0.001256 | 0.514 |
| 0.46 | 387.159 | 387.8533 | 0.001346 | 0.613 |
| 0.50 | 420.825 | 422.3967 | 0.001403 | 1.331 |
| S1_REFERENCE | 481.200 | 482.5233 | 0.001500 | 1.048 |
| 0.80 | 673.320 | 674.4867 | 0.001772 | 0.782 |

N0=79,353，机制检查的波动尺度来自二项方差；不是固定百分点容差，不要求翻转人数精确命中期望。S1平均tau=0.0057330640210697245，平均估计=0.005762257348015492。其检出率283/300及Wilson区间是条件于固定历史数据/分配机制的模拟精度，不是现实政策成功率。

核心计算计时15.66秒；测试、读取、计算和校验至收据组装约16.94秒，均为运行记录，不作性能比较。峰值内存not_measured。运行前空闲543,227,486,208 bytes，收据检查时543,225,212,928 bytes，减少2,273,280 bytes（同期磁盘变化，不等同新产物大小）；运行采样点约505.92 GiB，超过150 GiB下限。运行中每25轮检查预算，预留16 MiB容纳代码、文档、共享CSV副本及收据，远低于512 MiB。收尾清点POWER目录与全部变更文件（含被修改旧文档全长）约2.67 MiB，仍保留额外空间余量；当时空闲约504.53 GiB。

逐轮CSV：2,700行、1,276,231 bytes，SHA256 `7a4c03fd8a1ea016404592486f038db85f8ae1790b737685e9bbcd49789d30ec`；汇总CSV：9行、5,436 bytes，SHA256 `de7b2703c1db2fddd6edc36d9e92642c3d1ec9e6119c92026f5624073e624c6e`。共享副本与本地完成产物哈希一致。无图依赖安装；业务表保留全部9档和两类检出率。

T5.7仅raw先行子项完成，整体保留in_progress；T5.6/CUPED和raw/CUPED比较未执行，T7.3/G2本人待验。旧p=.0712结果、AA、COST-01和其他历史报告不改。下一步需本人解释：功效与成本门槛的区别、理论MDE与网格交叉的区别、旧单次未显著为何不能被本轮曲线替换；本轮不自动继续其他模块。
