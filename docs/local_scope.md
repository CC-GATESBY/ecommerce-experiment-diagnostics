# 本地小批量范围补充

记录日期：2026-09-16。依据：用户当前明确指令与本地 bootstrap；本补充优先于旧规划中相冲突的规模和执行顺序，v4 统计口径与质量要求保持不变。

1. **唯一主环境为本地。** MRC、Nectar、Spartan 不作为当前依赖，不登录或操作云资源。
2. **先工程样本，再分析样本。** 工程样本检查读写、解析与质量边界；分析样本须覆盖问题所需的完整观察窗口。
3. **全量不自动执行。** 全量双月或七个月处理均为可选扩展，须小批量验收后由用户明确决定，不作为首版完成条件。
4. **样本必须可追溯。** 记录实际规模、时间范围、采样方法、随机种子与 `scope_id`。文件头工程样本不能支撑总体行为、留存或长周期异动结论。
5. **分析样本优先保留完整轨迹。** 优先考虑跨完整观察窗口的固定用户样本。小输出不意味着来源下载或抽取扫描也小；每次按当轮授权先核对并说明下载、解压与抽取成本。
6. **只报告实际范围。** README、任务状态、报告和简历均使用实际运行记录，不套用数据集发布规模，不预填收益、性能或模型结果。
7. **三类证据隔离。** REES46 行为日志、人工模拟和 Criteo 公开实验分别处理，不连接两份数据的用户，不将离线证据写成线上业务效果。
8. **保留现有规划。** 三份 v4 文件继续使用；不复制另一套完整规划，不重排全部任务。

历史阶段 S0 已完成：独立 Git、指定 private 仓库、安全文件提交、push 与 SHA 核对。当时 T0.3 仅记录 Git 子项，安装、版本锁和 Spark 测试为 `not_run`。T0.1、T0.2 的既有结果保留，该历史限制不代替最新用户授权。

当前每轮只执行最新用户明确指定的任务，安装、下载和远程写入须有当轮授权。历史阶段 T0.3 的专用 Python 3.11 / JDK 17 / PySpark 3.5.8 环境、配置校验与人工数据 Spark smoke 已通过工程验收；证据保留在 `docs/t03_validation.md`，不覆盖 T0.2 快照。

历史阶段 T0.4 首个 REES46 文件子任务已核对来源，获取并登记 `2019-Oct.csv.gz` 和解压 CSV，保留两份各 100,000 条的顺序工程样本。当轮下载预算与验收保留在 `docs/t04_rees46_validation.md`；头部样本仅用于工程调试。

历史阶段 T0.4 补充顺序调整：Criteo 来源验收暂后移、不取消；先流式核验已登记的完整 10 月 CSV 覆盖，并按冻结的 5% 用户哈希规则准备整月 `user_sample_candidate`。这是输入准备，不是 T1.5 正式扩量验收。后续 REES46 的受控工程解析可按该数据线验收独立推进，不必等待 Criteo；整个 T0.4 与项目总 G0 仍不能提前勾选。本轮新增输出不超过 10 GiB，至少保留 150 GiB 空闲，不重新下载、解压或安装，不运行正式 ETL、指标、漏斗、实验或模型。下一步应先用旧 100,000 条工程样本验证 T1.1，再获授权处理整月用户候选样本。

本地目录末尾空格保留，运行时以实际 `<PROJECT_ROOT>` 为准；不从旧提示词中无末尾空格的路径创建另一份目录。

历史阶段授权（T1.1 工程子任务）：仅解析清单第一份 100,000 条旧工程样本，保留全部原始字段与质量标记；用人工数据、独立标准库预期与两个独立 run 核验。不得读取整月候选或重扫完整源，不安装或修改依赖锁。REES46 分数据线放行继续有效，Criteo 后移但保留；T0.4、T1.1 整体、T1.2–T1.5 与 G0/G1 不提前完成。最终证据见 `docs/t11_engineering_validation.md`。整月候选须后续明确授权。


历史授权（T1.1 整月候选解析）：先修复非有限值符号和字符串终止边界，回归旧 100,000 条样本；通过后仅解析已登记的 2,114,081 条整月用户候选。保留旧报告与输出，仍使用 Decimal(18,2)、local[4]/4g/UTC。全月及逐日结果只作为质量核对；不做 T1.2 去重/session 分析、正式指标或实验。新增产物累计上限 20 GiB、至少保留 150 GiB 空闲，最多一次必要整月重跑。Criteo 后移但保留，输入仍为 user_sample_candidate，T1.2–T1.5 与 G0/G1 不自动完成。该阶段证据记录于 `docs/t11_month_validation.md`。

历史授权（2026-09-17，T1.1 收尾）：仅实现日期/用途质量规则及受控事实读取，并以不超过 100 条人工记录验证同分析序列跨月追加与防重复登记。复用 `month-v101-01` 的既有事实做一次必要读取，`month-v101-02` 仅作复验；禁止重解析真实 CSV 或生成整月事实副本。新增本地产物上限 2 GiB，至少保留 150 GiB；不安装、下载、修改抽样/解析契约或进入后续模块。工程要求已通过，T1.1 标为 done（工程验收，本人解释未代验）；当时 Criteo/T0.4、T1.2–T1.5 与总 G0/G1 保持原状态，候选不升级为最终范围。证据见 `docs/t11_closeout_validation.md`。

历史授权（2026-09-17，T1.2）：只经既有 FactReader 与登记读取 `month-v101-01`，核验 2019-10-01 至 10-31 UTC 的重复候选、有效复合会话键及三种敏感性假设。先冻结 `reports/quality_policy.md`；真实 >5000 阈值不随结果调整。主口径保留全部合格事件，B/C 不回写或登记为正式输入。人工测试累计不超过 100 条，新本地产物上限 5 GiB，保留至少 150 GiB。不得读真实 CSV、重建事实、改 T1.1 契约/规则/登记证据或安装依赖；本轮不进入 T1.3。Criteo/T0.4、T1.3–T1.5、总 G0/G1 和本人解释项保持未完成。

历史授权（2026-09-17，T1.3）：仅用原 FactReader 读取 month-v101-01 的10月已验收事实，按冻结指标契约建设 first_seen、user_daily、daily_metrics、daily_dim。保留全部合格事件，品牌参考10月1–7日冻结Top200，按日期/用途继续执行既有门禁。人工18条测试先行，真实只运行一次，四层写读及433项核验通过；T1.3 done（工程验收，本人解释未代验），证据见 docs/t13_validation.md。累计新产物上限10 GiB、保留150 GiB；真实CSV、输入登记、原覆盖/质量表、既有事实及契约不改，不下载或安装。候选不升级为最终范围，T1.4/T1.5、Criteo/T0.4及G0/G1未完成；下一项唯一建议T1.4，不自动执行。

历史授权（2026-09-17，T1.4）：仅从已登记2,114,081条候选CSV独立计算，比较事实来源month-v101-01的metrics-month-01四层与品牌映射。核验整月并单列10月5日完整UTC日，属于核验覆盖扩展，不新增用户或月份。人工先通过并停止Spark后，真实DuckDB仅运行一次；不读完整源CSV、不重建真实事实/指标、不改旧登记和T1.3历史证据。仅新增官方DuckDB 1.5.5 wheel至.venv并导出实际锁；累计新产物预算10 GiB、保留150 GiB。四表及映射逐键无差异，快照重跑、日期扩展和失败重试通过；T1.4 done（工程验收，本人解释未代验），证据见docs/t14_validation.md。下一项仅建议T1.5冻结首版分析范围，候选不自动升级，Criteo/T0.4及总G0/G1保持未完成。

历史授权与范围决定（2026-09-17，T1.5）：冻结 `rees46_oct_user5_analysis_v1` 十月固定用户样本首版，引用原candidate scope、month-v101-01 / metrics-month-01 / crosscheck-month-01；不回写候选manifest。用于31天描述性分析、1–30日起始的24小时受限漏斗、22–31日具有至少3个同星期历史点的探索准备，以及10月1–14日前置/15–28日结果的离线实验窗口准备。历史数量不代表检测已验收，实验队列尚未确定；不能支持的日期/用途限制或标不适用，不调整阈值。原v4双月目标保留，11月和更大用户范围为后续可选扩展，不需自动下载或扩量来完成首版。本轮只核对收据、元数据和31行日指标，不启动Spark、不读CSV、不重跑ETL；512 MiB/保留150 GiB。T1.5 done（10月首版范围冻结；本人解释未代验），证据见docs/t15_validation.md。REES46分数据线已具T1.1–T1.4工程证据，Criteo/T0.4及全项目G0/G1仍未全通过；下一项仅建议T2.1，不自动执行。


## T2.1 本轮补充（2026-09-17）

本轮按明确授权完成行为覆盖与24小时顺序漏斗，沿用T1.5首版、唯一month-v101-01和原FactReader，不改写T1.5历史冻结记录。规则先写入reports/funnel_definition.md，再人工验证、最后真实只读一次。覆盖按31天事件/去重用户；路径按scope/user/session/product全月一个最早view，正式起点1–30日，共用24小时截止点；31日继续作结果观察，右截尾和同秒不确定分别审计。价格只取最早view，异常/冲突归unknown。

本轮done（工程验收，本人解释未代验）：1,382,516路径，formal 1,341,998，right_censored 40,426，order_uncertain 92；购买事件审计37,019与既有指标一致。输入关联、按日/价格桶和全内容Parquet读回均通过。人工86事件、33路径、303检查和31项配置/日期回归通过；首次人工空输入失败修复后留痕，真实623检查一次通过。实际结果与限制见docs/t21_validation.md。

新增数据仅在.local/t21，预算10GiB、保留150GiB；原事实、指标和绑定证据未修改。未读任何真实CSV、未重建事实/指标、未扩量或安装依赖。本轮未进入T2.2/T2.3、异动或实验；Criteo/T0.4及全项目G0/G1不提前完成。下一项唯一建议T2.2行为分析与阶段业务总结，不自动实施。

## T2.2 本轮补充（2026-09-17）

按本轮授权，复用唯一metrics-month-01的小型日/品类表和funnel-month-01汇总，先冻结reports/behavior_analysis_contract.md，只新增一次经FactReader的month-v101-01 UTC小时聚合。固定切片为日、首次view价格带、category_l1、UTC小时；不执行T2.3、T4归因/异常、实验或新切片。主口径baseline_keep_all，原输入、口径、范围与历史报告不改。

已形成reports/behavior.md、period_summary.md、4份新脱敏CSV与8张独立Matplotlib图；先行39项单元/回归、2项报告补验、894项准备、97项小时及791项独立CSV检查通过，实际图逐张检查。整项T2.2仍in_progress：品类日users/buyers不可跨日相加为月人数，本轮事实授权只限小时，当前只提供user_days/buyer_user_days，月去重not_measured，待用户确认范围。不扩大读取来隐式填补缺口，也不降低验收标done。技术证据见docs/t22_validation.md。

一次Spark、一次物理事实加载、24行小时结果；没有读源/候选/工程CSV、安装依赖、重跑旧ETL/指标/漏斗。绘图使用既有Matplotlib环境，只消费安全CSV；专用Spark环境和依赖锁未改。本地日志/配置/收据置于忽略目录，新增上限2GiB、保留150GiB，原始和用户级数据不提交。T2.3、T4.1–T4.4仍not_started；Criteo/T0.4和全项目G0/G1按原状态，本人解释不代验。下一项唯一建议T2.3一页业务决策备忘录，不自动执行。
