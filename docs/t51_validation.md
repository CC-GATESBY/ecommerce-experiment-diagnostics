# T5.1 固定队列与分母验收

2026-09-21，`cohort-oct-01`：**done（固定队列与分母验收；本人解释未代验）**。起点为 `309a151e9808f7f028e2074c8b040eaea56a0c06`。T4追加调查本轮结束，旧结果保留；T5.2–T5.5未执行。

## 输入和发布

唯一分析范围 `rees46_oct_user5_analysis_v1`，数据scope仍为 `rees46_2019_oct_user5_fedd938409b5f836_20260916_v1`，事实run `month-v101-01`，指标run `metrics-month-01`，独立核验run `crosscheck-month-01`。经原快照选择器选择明确的 `agg_user_daily` 文件，确认322,660行元数据、逐行来源字段及与T1.4绑定的文件SHA。只加载用户日表，28天子集用于本次计算；不读取事实内容或任何原始/候选/工程CSV。

复用原scope校验、完成证据核对和日期门禁；旧manifest差异只采用已经审查的Criteo独立条目扩展兼容规则，没有放宽快照或输入身份。前置与结果窗口分别为10月1–14日、15–28日UTC，半开边界固定；28天计数与金额状态都与指标、独立核验及登记相符。运行前规则稿指纹和原文保留在本地，运行后仅为新业务文档补充实测结果。

队列版本 `rees46-pre-oct01-14-post-oct15-28-v1`。SQL先分别按用户聚合两个窗口，再以前置名单左连接结果。先写独立staging、全部核对通过才发布complete。身份与数值两份Parquet各84,165行；共同稳定键连接后仍是每用户一行。金额Decimal(38,2)，二元标记INTEGER，事件数量BIGINT，用户ID不进入数值表。SHA键不等于匿名公开许可，也没有用于随机分组。

## expected / actual / pass

| 检查 | expected | actual | pass |
|---|---|---|---|
| 人工名单与分母 | A/B/C入组；返回2、未返回1、买家1；结果期新出现D/E不入组 | 完全相同，B/N=1/3、B/R=1/2 | 是 |
| 边界与重复 | 10月1日00:00纳入pre，15日00:00纳入post，29日00:00排除；重复购买2条/1买家/20.00 | 相同；多事件不增加人数 | 是 |
| 金额与质量 | 坏金额不补0，不删买家；partial/unknown/窗口阻断区分；缺日或计数阻断拒绝 | 相同；无返回时B/R为NULL | 是 |
| 输出保护 | 同目录不得覆盖；读回类型与全部字段多重集一致；身份/数值连接不放大 | 拒绝覆盖，双向差异0，schema相同 | 是 |
| 真实入组名单 | 与前置期独立DISTINCT名单双向差异0 | 0；84,165用户及稳定键均唯一 | 是 |
| 返回守恒 | R+(N−R)=N；B≤R≤N | 33,442+50,723=84,165；4,812≤33,442≤84,165 | 是 |
| 独立结果期计数 | 用户日表以独立前置名单筛选后重新去重/计数 | 返回33,442、买家4,812、购买事件10,630，全部相同 | 是 |
| 前置期数量/金额 | 同范围用户日加总940,216事件、5,149,665.11金额 | 精确相同 | 是 |
| 结果期队列数量/金额 | 独立筛选用户日570,986事件、3,499,563.53金额 | 精确相同；金额未知用户0 | 是 |
| 已有证据不变 | 所选指标文件和绑定收据前后SHA相同，事实元数据不变 | 相同；旧报告未修改 | 是 |

4项小型人工测试覆盖这些情景，先通过才读取真实用户日；入口再次执行相同轻量测试以绑定当前代码。未重跑历史真实任务。独立预期来自直接查询用户日的DISTINCT/半连接及金额加总，不从生产队列汇总复制；它是本次变换核验，T1.4历史独立CSV核验仍为上游证据。本轮不声称重验上游所有日志。

两种比例由同一个4,812计算：5.7173%与14.3891%，差8.6718个百分点；没有使用购物车或Criteo率作为预期。另有57,527名结果期才在观察范围出现的用户被排除于队列。结果期完整观测金额3,499,563.53，每入组用户41.5798，均含未返回者的结构性0；日志金额不等于利润。

## 运行与本地文件

现有Python 3.11.15 / DuckDB 1.5.5，4线程、512MB引擎内存限制、UTC、最多256MB临时空间。没有启动Spark、安装依赖、下载数据、重建事实/指标、生成稠密用户日面板或读取商品明细。只向Python收回28行日期核对与标量，未collect用户清单；完整队列在DuckDB中写入。

本地目录 `.local/t51/cohort-oct-01/complete/` 内：`cohort/identity.parquet` 3,305,506 bytes、`cohort/values.parquet` 3,168,318 bytes；合计6,473,824 bytes。身份SHA `3f2041b1fc9bf879d0f29737be75085830a189ce8ffea871bcad3049bcc36cdd`，数值SHA `d361a8660b7e8f4bd40bc4bc5b7a54e2bf4c74343990fcf338c7d22d9a266e97`。本地收据记录schema、输入文件、校验、代码指纹与版本；日志和用户级文件均被忽略。

真实入口0.946秒（包括人工测试、证据/文件核对、用户日计算与输出检查，至写完成收据前），仅执行记录，不作跑分。收尾本地新增约6.6MB，预算1GiB；运行采样最低空闲544,491,565,056 bytes，超过150GiB。磁盘可用量受其他进程影响，不把差值当本任务精确占用；输出实际字节另列。峰值内存 `not_measured`，未额外运行测量。

在保留尾空格的项目根目录执行：

```sh
.venv/bin/python -m unittest tests.test_cohort -v
# 真实复跑需当轮授权；新run ID不得已存在，仍只选原canonical快照。
.venv/bin/python scripts/build_cohort.py --run-id cohort-oct-NEW
```

使用既有被忽略的 `config/analysis_scope.local.json` 定位配置和收据，不新建登记系统。共享代码、SQL、脱敏单行CSV及文档可以提交；身份、数值、日志、机器路径仅本地。没有未解决的工程差异。用户需自行解释固定分母、条件比例和购物车资格差别；下一阶段另行授权稳定分组与方法验证，本轮停止。
