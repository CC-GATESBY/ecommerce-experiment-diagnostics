# 项目协作约束

- 开始前读取 `CODEX_START_v4.md`、`plan_v4.md`、`TASKS_v4.md` 与 `docs/local_scope.md`。用户当前明确指令优先；本地范围补充优先于旧规划中的冲突规模与顺序。
- 每轮只执行最新用户明确指定的任务；安装、下载和远程写入必须在当轮授权范围内。已完成任务和历史证据不重复生成，未通过的验收不得提前标为完成。
- 使用实际项目根目录；当前本地目录名末尾有一个 U+0020 空格。路径始终正确引用，不自动重命名，不在父目录初始化 Git。
- 唯一主环境是本地小批量；全量扩展须用户另行决定。环境隔离在项目内，不改全局默认环境；不得进行超出当轮授权的安装、下载或云资源操作。
- 原始数据不可变；三类证据隔离，不连接 REES46 与 Criteo 用户。统计口径与质量验收遵循 v4，不为通过测试修改结果。
- 不虚构业务、性能或实验成果。未运行写 `not_run`；用户本人解释项不得代为勾选。
- Git 保持 public/main（用户主动公开，2026-09-23确认）；不恢复 private，不因公开状态阻止已授权的正常推送，也不擅自修改可见性。使用现有作者配置，先检查历史、origin、认证与远程状态，逐文件审核并明确暂存；不使用 `git add .`、`git add -A` 或 force-push，不自动覆盖现有历史。
- 原始数据、环境日志、本机绝对路径、主机名、身份邮件、凭据及本地 bootstrap 不进入提交文档。安全汇总和配置模板审核后才可提交。
- 完成授权任务后核对本地与远程 SHA，报告实际文件及未运行项并停止。文档中文，代码和脚本注释英文。

历史阶段：S0 仅完成 Git 准备与首次同步，当时未安装依赖或验证 Spark；T0.1、T0.2 的原始文档与环境快照保留。该阶段的临时限制不代替最新用户授权。
