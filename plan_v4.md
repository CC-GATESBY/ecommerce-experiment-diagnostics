# 电商增长分析与实验可信性验证 — 优化执行规划 v4

**版本日期：2026-09-16｜状态：执行规格，尚未实施验证｜目标岗位：拼多多 2027 届数据分析师**

## 阅读顺序

先看第1–4节明确范围与口径，再按任务依赖执行第5节。每天用同目录 `TASKS_v4.md` 更新状态；给 Codex 的约束与首轮任务见 `CODEX_START_v4.md`。本包没有项目运行代码，也没有预填实验或性能结果。

## 1. 修订依据与项目定位

**来源保留：** 以用户上传的《电商增长分析与实验可信性验证—完整执行规划（v3，6天）》为基础，保留 REES46 2019-10/11、Criteo v2.1、Spark SQL、指标诊断、A/A、CUPED、Hadoop 实践与可选 Uplift 主线。没有把更早的七个月/疫情/Temu手册作为当前规格。

**本版新增或改变：** 以下任务顺序、优先级、实验设计、业务备忘录、验收规则及资源保护，属于针对 v3 的优化建议，不是源文件已完成的内容。外部方法与软件依据列于文末[S1]–[S10]。所有阈值、资源预算、抽样规模都是规划参数，不是实测结果。

**项目的一句话目标：** 用 Spark SQL 构建可核验的电商行为指标，解释业务变化；用离线实验校验与独立公开随机实验来源数据评估分析方法，再形成可执行的业务核查和下一轮实验建议。

### 1.1 三类证据分开

| 数据/场景 | 允许回答 | 不允许声称 |
|---|---|---|
| REES46原始日志 | 观察到的行为、路径、金额与结构变化 | 策略造成了增长；日志金额等同财务收入 |
| REES46离线A/A与人工注入 | 指定数据与设计下方法的校准、检出与故障定位 | 实际上线策略有效；验证了不存在的线上分流/曝光系统 |
| Criteo修正版 | 公开基准上的组间结果差、增量排序与实验评估方法 | 原始广告主真实收益；补贴政策效果；与REES46同一批用户 |

不要连接两份数据的用户。复用代码接口、结果格式和质量标准，不虚构统一业务身份。Criteo是随机广告实验来源的数据；发布与抽样限制仍应保留，不能简单外推原始业务因果量。[S4]

### 1.2 对应JD的最终证据

| 职位需求 | 本版交付 |
|---|---|
| SQL/Python、数据处理 | 事实表、指标SQL、独立DuckDB核验、重跑测试 |
| 电商业务理解与行动建议 | 行为/路径分析、阶段总结、一页决策备忘录 |
| 异动监控与定位 | 候选告警、可加总分解、切片贡献、观察案例 |
| 实验设计和效果评估 | Criteo评估、固定人群的离线A/A、下一轮实验方案 |
| Spark/Hadoop加分项 | 执行计划与测量；实际HDFS读写和YARN业务作业 |
| 沟通能力 | 面向业务的备忘录与口头解释；真实跨团队协作仍由实习/校企经历证明 |

## 2. 优先级与完成门槛

- **P0：投递核心。** 完成数据、业务、公开实验、基础A/A/注入、真实案例、复现与个人理解验收。
- **P1：完整增强。** CUPED、功效、故障验证、Hadoop和性能证据。完整目标版本=全部P0+P1。
- **P2：延后扩展。** Uplift与可选云迁移；不阻塞首次核心成果。

Hadoop放在P1是为了不让安装阻塞分析，不是删除目标。没有完成T6.2就不能写Spark on YARN；没有完成T5.6就不能填写CUPED降方差数字。求职材料仅描述实际完成范围。

### 2.1 里程碑（不是硬性日历承诺）

| 门槛 | 必须看到的证据 | 下一步 |
|---|---|---|
| G0 可开始计算 | T0.1–T0.4完成；小型Spark写读通过 | 运行真实工程样本 |
| G1 数据可信 | T1.1–T1.4的样本核验通过 | 扩量与分析 |
| G2 核心可展示 | 所有P0通过，报告/代码/本人解释一致 | 可按已完成内容准备投递 |
| G3 完整目标 | P0+P1通过，有YARN/CUPED/故障与性能证据 | 更新增强版简历 |
| G4 可选深度 | P2相关模块完成 | 增加Uplift或云运行记录 |

不能因“到了第5天”自动变更完成状态。任务状态统一：`not_started / in_progress / blocked / implemented_unverified / done`。`done`必须同时有产物和验收证据。

## 3. 环境、范围与数据管理

### 3.1 已知与待确认

用户确认本地可用磁盘600多GB。v3写明M2 Pro、16GB内存，本版把它当资源规划假设，实际值由T0.2记录。MRC是否可用尚未有登录后配额与授权证据，因此它不是开工依赖。

首版默认本地Spark；建议Python3.11/JDK17/Spark与PySpark3.5.8，这是兼容起点而非最新版本声明。[S1] 初始 `local[4]`、Driver 4GB只用于小样本预算，后续看实际峰值调整。Hadoop在独立阶段锁版本并验证，不能将包名安装命令当作兼容性证明。

### 3.2 独立配置“计算”与“存储”

```yaml
# 建议配置规格；须在T0.3实现读取与校验，不是已可执行代码。
profile: local
compute:
  mode: local
  master: "local[4]"
  driver_memory: "4g"
  shuffle_partitions: 32
storage:
  backend: local
  raw_root: null       # 由使用者在本机填写绝对路径
  warehouse_root: null
  temp_root: null
  simulations_root: null
metrics:
  version: "v4.0"
  session_timezone: "UTC"
  duplicate_policy: "flag_only"
  money_parse_policy: "decimal_from_string"
resources:
  driver_collect_budget_mib: 512  # 受控小表建议上限，非内存安全保证
  min_free_disk_gib: 150         # 用户可调整的保护线，非系统最低要求
```

云环境新增profile即可，不把真实SSH主机、密钥、token写入公共配置。Spark内存参数必须在启动阶段生效。每项大任务运行前检查内存、磁盘和输入规模；16GB机器不同时进行Hadoop集群测试与大模型训练。

### 3.3 集中收集与存储规则

事实表禁止 `collect()` / `toPandas()`。用户级统计表也先估计列类型、内存及副本开销；超过预算就按固定用户抽样、批次/充分统计量计算或换获准资源。没有“少于500万行就安全”的标准。[S2]

原始区不可变；文件SHA和处理清单防止重复导入。写入先到运行专属staging，验收后发布；按批次/日期切换，不能以全根目录overwrite误删其他月。保留来源与结果登记，临时文件可清理但不能删除唯一原始副本。

资源预算按单月峰值决定，不能保证600GB必然足够任意副本/中间结果组合。全量双月是扩量目标，不是发布规模的预设；资源不足时使用跨双月固定用户样本，明确其估计范围。首版不处理七个月。

## 4. 数据与统计契约（先定口径再写代码）

### 4.1 基础范围与金额

主业务范围需同时有可解析UTC时间、有效user_id、允许的event_type；被排除的记录仍在事实/隔离层可核对。缺session不影响用户层的其他合格事件。金额从price字符串直接解析为Decimal：先检查原始小数位和范围，再在契约中锁精度；不可静默截断额外小数位。零/负/缺失价格分开报告。价格带以原始price单位表示，不臆造人民币或其他币种。

初版 `purchase_amount` 是合法购买事件价格的观测总和，明确日志口径与金额完整性。购买金额未知不应装成真实0。若金额质量不合格，继续提供计数结果，但暂停该日期的金额诊断或明确降级为部分观测金额分析；实验金额分支不得因结果缺失随意删掉分组用户。

| 字段 | 口径 |
|---|---|
| active_users U | 同一合格时间范围内出现的不同用户数 |
| buyers B | 其中至少一条purchase事件的不同用户数 |
| purchase_events | purchase事件记录数，不是订单数 |
| purchase_sessions | 合格(user_id,session_id)且含purchase的不同会话数；不声称有真实order_id |
| purchase_amount V | 已定义的日志购买金额；与计数使用同一用户与日期范围并附完整性信息 |
| buyer_rate R | B/U；U=0时为null |
| amount_per_buyer M | V/B；B=0时为null；不是订单平均金额 |
| is_first_seen_day | 在当前完整观察范围内首次出现，不是注册或获客 |

只在U>0、B>0且金额质量允许时检查 `V = U × R × M`。计数和Decimal总额严格核验；浮点比率用事先写入测试的绝对+相对容差。全量与用户样本的同名指标必须携带`scope_id`，不可混比。

### 4.2 表粒度与切片

`fact_events`：一条原始日志记录。`agg_user_daily`：UTC日×用户。`agg_daily_metrics`：UTC日×scope。`agg_daily_dim`：UTC日×scope×一个维度×值。unknown必须占一档；同一维度金额可相加，用户数跨品类不可相加，不同维度更不能相加。Top品牌映射在参考期冻结，不能每天变化造成伪异动。

`funnel_paths`：用户×会话×商品，不称为天然用户漏斗或订单漏斗。先找合格view，再找其后的cart与purchase；有完整三步路径时优先归入三步路径，否则标记未观察到中间cart的直接路径。绑定首次view的属性、首次view UTC日期和固定跟踪窗口。同秒没有可靠顺序时标记不确定；末端观察不完整不判定为流失。

### 4.3 统计与验收两层分开

**工程硬验收：** 输入/输出对账，已知小样本结果、函数边界、数据隔离、测试集隔离、同种子复现、同样本跨环境结果。失败必须修复或明确阻塞。

**研究结果：** 实际误报、SE、效应、功效、模型排序和性能收益。可以不显著、不改善或不优于基线；按预设方法报告不确定性及局限，不能改数据/换种子直到满意。

A/A的近似均匀p值和约5%的误报比例是校准目标，不是任意有限数据下保证的结果。[S5] CUPED主方差指标针对处理效应估计：`ratio=SE_adj²/SE_raw²`，`reduction=1-ratio`；后者允许为负。结果变量方差比只能另列辅助。[S6]

正式功效模拟先生成不受分流影响的潜在结果，再分流观察，清楚区分目标平均效应、实现的有限样本效应与单次估计差。模型/test评价同样禁止“必须胜出”式验收。[S8]

### 4.4 关键算法规格（避免执行时再猜定义）

**均值差与比率指标。** 统一方向为B−A。Welch报告两组用户级均值及均值差；常量或不足样本返回方法状态。对于用户级成对(X,Y)，`r=sum(X)/sum(Y)`，可用等价残差公式估计：

`Var(r_hat) ≈ sample_var(X - r_hat*Y) / (n * mean(Y)^2)`。

两组独立时差值方差为两组方差之和；若组内同一用户多行，必须先聚合，不能把事件当独立单位。这个方差是ratio-of-sums的近似，不是给每个用户算X/Y再取平均。[方法实现按T5.3核验]

**CUPED。** 初版共享`theta=cov(X,Y)/var(X)`和`mean(X)`，`Z=Y-theta*(X-mean(X))`；通过Z计算B−A、SE与CI。报告`SE_adj^2/SE_raw^2`和`1-SE_adj^2/SE_raw^2`；后者才是估计的方差减少比例。合并样本拟合是首版大样本近似，不夸大为有限样本严格无偏；用独立构造样本与A/A检查。[S6]

**正式功效模拟。** 固定一份符合入组规则的用户集合及其Y0。对Y0=0的用户，独立生成Bernoulli(q)，其中`q=delta/(1-p0)`，从而得到潜在Y1；再独立分配A/B，观察A的Y0与B的Y1。每轮保存`mean(Y1-Y0)`及目标delta。模拟随机源与分组随机源隔离；不得在看到某轮A/B结果后重新定义效应。预先指定主检验与双侧alpha=0.05，说明这些是演练参数。

**Qini（仅E2）。** 固定测试集N个用户、模型分数降序，分数并列时以与标签/处理无关的固定哈希顺序打破；对前`k=ceil(p*N)`个用户，记处理/对照人数为`N1(k),N0(k)`、转化数为`C1(k),C0(k)`：

`G(p)=C1(k)-C0(k)*N1(k)/N0(k)`。

本版固定随机参考线`B(p)=p*G(1)`，固定横轴为覆盖比例p，主评分为梯形积分`A_Q=integral_0^1 [G(p)-B(p)] dp`，不除以“完美曲线”。采用预先固定网格（例如0、0.01、…、1），`G(0)=0`是约定；其余点若没有对照、或预先规定的两组最小人数不满足，标记不可用并停止该面积评分，不静默插值填成0。稀疏时应在valid阶段选择更粗公共网格后冻结，不能为改善test表现改网格。

该G的规模依赖发布测试集及其中处理人数，不是“全部目标用户都接受处理”后的真实业务转化数，也不能跨不同测试规模直接比较。Bootstrap在处理/对照组内分别重采样，固定模型、重新算排序指标；区间仅反映给定模型的评估样本不确定性，不包括重新训练的不确定性。候选覆盖率和模型选择由valid确定，test只作预定评价。若采用其他实现，必须另开指标版本，不能沿用同一个评分名称比较。

## 5. 完整任务清单

以下每项包含优先级、前置任务、实施范围、交付物、验收与个人理解检查。输出路径是未来要实现的项目内路径，并非本规划已经生成的分析结果。

### 0｜范围与环境


#### T0.1｜冻结目标、证据边界与首版范围

**级别：P0｜前置：无｜初始状态：not_started**

**做什么：** 建立项目简介和 JD 对照表。保留 REES46 2019-10/11 与 Criteo v2.1 两条独立数据线；不连接用户、不声称来自同一平台。明确行为诊断、离线模拟、公开实验评估三种证据。项目名称固定为“电商增长分析与实验可信性验证”。

**交付物：** `docs/project_brief.md`；`docs/jd_mapping.md`

**验收：** 每条岗位要求对应至少一项拟交付证据；所有尚未执行的结果标记 planned/not_run；没有“已处理上亿条”“已提升转化率”等预填成果。

**你需要解释：** 为什么使用两份数据？为什么真实行为变化不等于策略因果效果？

*与v3关系：重组 v3 项目定位；新增业务问题与证据分层。*


#### T0.2｜确认真实资源并选定一个主环境

**级别：P0｜前置：T0.1｜初始状态：not_started**

**做什么：** 记录操作系统、CPU 架构、物理内存、可用磁盘、Python/Java/Spark 版本。以本地作为不依赖审批的默认路径；MRC 只做权限、配额、用途授权核查，有明确可用资源后才迁移。按 v3 的 16GB 内存假设保守起步，不把它写成本次已检测结果。

**交付物：** `docs/environment.md`；`runs/environment_snapshot.txt`

**验收：** 环境记录来自实际命令；MRC 状态为 available/blocked/unverified 之一并说明依据；不共享密钥；不同时维护本地、MRC 和 Spartan 三套主环境。

**你需要解释：** 哪些资源属于本机，哪些属于云项目？为什么存储后端与计算运行方式要分开配置？

*与v3关系：替换 v3 T1.1 先装整套 Hadoop 的顺序。*


#### T0.3｜仓库、版本锁定与最小 Spark 验证

**级别：P0｜前置：T0.2｜初始状态：not_started**

**做什么：** 建立仓库与配置。建议起点为 Python 3.11、JDK 17、PySpark/Spark 3.5.8，并记录实际安装的精确版本；不是追随 latest。先用 local[4]、4GB Driver 运行小型 DataFrame 的写入/读回，不启用 Hive。提交器在启动 JVM 前设置内存与临时目录。

**交付物：** `config/local.yaml`；`requirements.lock.txt`；`scripts/smoke_spark.py`；`tests/test_config.py`；`.gitignore`

**验收：** 小型 Parquet 写入、读回及聚合均成功；执行器和 Python 包 Spark 版本一致；配置缺失时报错；raw、私钥、token、临时数据不入 Git。smoke 只能证明基本环境，不能代表全量性能。

**你需要解释：** local[4] 的含义是什么？为什么 JVM 堆不等于总进程内存？

*与v3关系：替换 v3 不锁定 brew 版本与固定 8GB/8 线程的设定。*


#### T0.4｜数据来源验收与不可变原始区

**级别：P0｜前置：T0.3｜初始状态：not_started**

**做什么：** 从原发布入口获取文件并核对使用条款。按文件记录 URL、版本、下载时间、字节数、SHA256、压缩/解压关系、解析行数、表头、时间覆盖。先获取需要的文件，不下载全七个月。为 Criteo 确认修正版标签和字段；用发布规模作一致性线索，不强行把实测改到参考值。

**交付物：** `data/MANIFEST.md`；`data/manifest.json`；`ingest/ingest.py`；`docs/data_sources.md`

**验收：** 原始文件可定位且校验值可复查；表头行与数据行分开；同一文件重复导入被识别；相同内容换名也不会重复累计。下载失败、缺月份或条款不明写明阻塞，不伪造数据。

**你需要解释：** 如何证明这次处理的数据是哪一版？为什么不能以文件名当作唯一标识？

*与v3关系：修订 v3 T1.2–T1.3。*


### 1｜可信数据与指标


#### T1.1｜解析事实表并保留质量标记

**级别：P0｜前置：T0.4｜初始状态：not_started**

**做什么：** 先用工程样本完成原始 CSV→事实 Parquet。保留原始字段与来源批次；显式解析时间和 UTC 日期。price 从原始字符串解析为预先核验精度的 Decimal，禁止先转 double 再转 Decimal。保留无效时间、无效用户、未知行为、缺失/负价格、缺失 session 的标记；日指标只读取冻结的合格范围，排除数量进入质量报告。

**交付物：** `etl/01_events.py`；`sql/ddl/events.sql`；`warehouse/fact_events/`；`reports/data_quality.csv`

**验收：** 输入记录数=事实层记录数（或有效事实+显式隔离记录数）；无静默丢弃；金额精度溢出有错误记录；只有通过质量门禁的日期进入后续分析。跨月追加不覆盖已有日期。

**你需要解释：** 哪些字段无效会阻止哪一种分析？为什么缺失 session 不应让该用户所有行为被删除？

*与v3关系：修订 v3 T1.4 的时间、金额和输出策略。*


#### T1.2｜重复候选与会话分布核验

**级别：P0｜前置：T1.1｜初始状态：not_started**

**做什么：** 默认主口径保留记录，不使用 dup_rank=1 自动去重。以完整规范化字段（包括金额）识别相同记录候选，仅作质量标签；文件级重跑由清单去重。有效 (user_id,user_session) 单独聚合事件数，再关联回明细；空 session 不进入同一个计数大组。5000 只作为沿用 v3 的探索阈值，不自动认定机器人或无效流量。

**交付物：** `sql/quality/duplicate_candidates.sql`；`sql/quality/session_profile.sql`；`reports/quality_policy.md`

**验收：** 缺失 session 的记录不会被误标为“共同的超大会话”；有关联前后行数/金额核对；去重/高频过滤只进入独立敏感性分析，报告相对主口径的差异，不污染正式表。

**你需要解释：** 候选重复与真正重复的区别？数据倾斜与数据无效为什么不能混为一谈？

*与v3关系：修订 v3 T1.4–T1.5。*


#### T1.3｜建设统一口径的指标层

**级别：P0｜前置：T1.1、T1.2｜初始状态：not_started**

**做什么：** 用 Spark SQL 建 first_seen、user_daily、daily_metrics、daily_dim 四层。范围、金额完整性、未知维度和比率分母按本文件“指标契约”统一。brand Top200 用基准期冻结映射，不每天重新排序；保留 unknown 与 other。用户数跨品类不可加，总额只在单个互斥维度内核对。

**交付物：** `docs/metric_contract.md`；`sql/metrics/`；`warehouse/agg_user_daily/`；`warehouse/agg_daily_metrics/`；`warehouse/agg_daily_dim/`

**验收：** 主键唯一；无分母时输出 null+状态而非无穷；满足条件的日期有 V=U×(B/U)×(V/B) 恒等式核验；每个维度金额加总等于同范围总金额；同一报告注明金额缺失范围。

**你需要解释：** 购买事件、购买会话、购买用户分别是什么？金额/购买用户和金额/订单为什么不同？

*与v3关系：修订 v3 T1.5，并新增完整性门禁与稳定切片。*


#### T1.4｜独立交叉核验与重复运行测试

**级别：P0｜前置：T1.3｜初始状态：not_started**

**做什么：** 先测试人工小样本，再抽取一个完整 UTC 日期的原始记录，用 DuckDB 独立实现相同主口径。不得直接读取 Spark 汇总结果当“独立验证”。测试同一批次重跑、追加另一日期和失败后重试。

**交付物：** `tests/crosscheck_duckdb.py`；`tests/test_idempotency.py`；`reports/crosscheck.csv`

**验收：** 行数、去重用户数、购买事件数一致；Decimal 金额按同一精度精确核对；派生浮点比率用预设容差。原始过滤范围不同必须解释并修复，不能通过扩大容差掩盖。重跑结果不翻倍，历史分区不消失。

**你需要解释：** 两个系统数字相等为什么仍可能一起错？什么叫独立实现与相同口径？

*与v3关系：修订 v3 DuckDB 原始口径对去重口径的冲突。*


#### T1.5｜按验收扩量并冻结分析范围

**级别：P0｜前置：T1.4｜初始状态：not_started**

**做什么：** 小样本通过后再跑完整日期、完整月、10/11 月。资源不足时，按固定用户哈希抽样并保留两个月完整事件轨迹；范围写成“抽样用户覆盖双月”，不宣称处理了全量。记录输入/输出规模、内存峰值、磁盘峰值、耗时和输出文件布局。

**交付物：** `config/analysis_scope.yaml`；`runs/etl_baseline.json`；`reports/scale_baseline.md`

**验收：** 范围包含足够的历史日期以支持拟用基线；每次扩量仍通过核心核验；运行规模均来自记录。头部行样本只作工程测试，不用于总体行为结论。

**你需要解释：** 为何用户抽样保留轨迹，而逐行抽样会改变会话与转化路径？

*与v3关系：将 v3 Day1 亿级全量改成分级验收。*


### 2｜业务分析


#### T2.1｜区分行为覆盖与顺序漏斗

**级别：P0｜前置：T1.5｜初始状态：not_started**

**做什么：** 保留浏览/加购/购买行为覆盖人数。另按 (user_id,user_session,product_id) 建会话内商品路径，明确严格时间顺序、跨日归属、观察窗口、同秒不明顺序和右侧截尾；路径表的日期取首次有效 view 日期。初版使用明确的24小时最大窗口作为设计选择，并排除后续观察不完整的路径。价格带按首次 view 的价格，不能用购买后的价格给全漏斗分组。

**交付物：** `sql/metrics/funnel_paths.sql`；`reports/funnel_definition.md`；`warehouse/funnel_paths/`

**验收：** 路径数不误写成去重 session 数；view→cart→purchase 与未观察到中间 cart 的 view→purchase 分开；没有 view 的购买另作覆盖统计；人工顺序、重复事件、跨日和末日样本测试通过。

**你需要解释：** 为什么三类行为人数不能直接相除当顺序漏斗？为什么“没观察到加购”不等于没有加购？

*与v3关系：细化 v3 T1.5、T2.1 的漏斗粒度。*


#### T2.2｜行为分析与阶段业务总结

**级别：P0｜前置：T2.1｜初始状态：not_started**

**做什么：** 围绕三个问题写报告：金额变化是流量/购买率/人均金额哪一部分变化；哪个路径值得核查；金额和用户集中在哪些切片。保留 v3 趋势、漏斗、品类集中度、小时分布四类图；每个不同纵轴单位使用独立图或明确面板，不能把不同单位混成一条轴。生成一期固定日期范围的阶段总结。

**交付物：** `reports/behavior.md`；`reports/period_summary.md`；`reports/figures/`

**验收：** 每张图附数据范围、粒度、来源表、分母及一句可验证结论；每个报告数字可追溯到CSV/Parquet；不将相关、价格带差异或首次观察用户解释成业务因果。

**你需要解释：** 给业务同事一分钟，最重要的发现是什么？它影响哪些人或商品？

*与v3关系：保留 v3 T2.1，新增 JD 日报/阶段总结交付。*


#### T2.3｜完成一页业务决策备忘录

**级别：P0｜前置：T2.2｜初始状态：not_started**

**做什么：** 用一个实际发现写“问题→证据→替代解释→优先核查→拟验证措施”。按影响范围与验证成本排序；库存、履约、投诉等源数据没有的字段写成后续采集需求。T4.4完成后更新该备忘录，保留初稿和修订原因。

**交付物：** `reports/business_decision_memo.md`

**验收：** 至少一条建议含具体对象、下一步操作、所需证据和验证方式；不只写“加强关注/提升转化”；不得凭空填写收益。证据不足时结论可以是暂不调整策略、先补数据。

**你需要解释：** 为什么先做这项而不是其他项？什么新证据会改变你的建议？

*与v3关系：新增：补齐 v3 业务行动不足。*


### 3｜公开实验与实验设计


#### T3.1｜Criteo 验收并提前封存划分

**级别：P0｜前置：T0.4、T0.3｜初始状态：not_started**

**做什么：** 独立读取修正版 Criteo；核对 treatment、visit、conversion、exposure 与 f0–f11 的实际字段。创建稳定行身份：使用源文件标识和确定性解析顺序，或明确处理相同记录组的固定哈希，不能使用不稳定 Spark 分区行号。按 treatment 做60/20/20固定划分，记录全部清单；探索和建模都不得利用 test 标签选择方案。

**交付物：** `uplift/ingest_criteo.py`；`uplift/split.py`；`reports/criteo_manifest.md`；`data/criteo_split_manifest.json`

**验收：** 三份记录身份不交叉，固定输入/种子重跑相同；核对每份样本量和标签率；约0.85仅是官方舍入后的比例参考，不视为精确分流配置来硬做 SRM。

**你需要解释：** 为什么先封存测试集再探索特征？为什么Criteo不能和REES46按用户拼接？

*与v3关系：提前 v3 T6.1；修订 T4.1 之前的数据使用纪律。*


#### T3.2｜先完成真实实验来源数据的总体评估

**级别：P0｜前置：T3.1｜初始状态：not_started**

**做什么：** 用 Spark 聚合各 treatment 的 n、visit总数、conversion总数。预先将 conversion 设主结果、visit 设辅助，报告绝对差、相对差、标准误/置信区间。处理组名称指广告处理资格；不按 exposure=1 筛选。总体统计可以使用全量，但不得借此做测试集异质性探索或选模型。

**交付物：** `uplift/ate.py`；`reports/criteo_effects.csv`；`reports/criteo_ate.md`

**验收：** 分母明确、低频条件检查、独立小样本验证通过；报告名称优先用“公开基准的组间差异/增量估计”，并说明RCT来源与发布抽样限制，不能恢复原广告主收益或把它当补贴效果。

**你需要解释：** 为什么看 treatment 而不只看实际曝光者？CI跨零与证明无效果的区别？

*与v3关系：保留并提前 v3 T4.1；强化公开抽样数据的解释限制。*


#### T3.3｜有限的描述性异质性分析

**级别：P1｜前置：T3.2｜初始状态：not_started**

**做什么：** 只在 train/valid 中，按预先写下的规则选最多两个匿名基线特征，使用训练集分位边界切片，合并重复分位断点，报告每组样本量、结果差和区间。没有字段含义就只写 f列名，不臆造年龄或收入。

**交付物：** `uplift/hetero.py`；`reports/criteo_heterogeneity.md`

**验收：** 切片只基于基线特征；稀疏组不输出虚假精确结论；显著性探索采用预定的多重比较说明；不把“A显著B不显著”当作两组效果显著不同。

**你需要解释：** 描述性分层、CATE建模、正式异质性检验有什么区别？

*与v3关系：收紧 v3 T4.2，并避免它泄漏到 Day6 测试集。*


#### T3.4｜编写下一轮业务实验设计

**级别：P0｜前置：T2.3、T3.2｜初始状态：not_started**

**做什么：** 选择备忘录中的一个可操作假设，写未上线的实验方案：处理内容、目标人群、用户级固定分流、入组与结果窗口、主指标/辅助/护栏、MDE、样本量、结束规则和决策规则。基准率从口径相同的历史窗口估计或明确列出规划假设；报告基准率/MDE的敏感性。先做双组固定期限设计，不增加序贯检验。

**交付物：** `reports/next_experiment_design.md`；`abtest/sample_size.py`；`reports/sample_size_scenarios.csv`

**验收：** 明确主指标是全部入组用户的结果而非仅活跃者；列出退款/延迟等尚未采集的护栏；样本量计算独立核对；alpha=0.05、power=0.8等是设计选择。不能把公开历史率当拼多多真实基准率，也不虚构每天可入组用户数。

**你需要解释：** 为什么随机化单位这样选？上线、继续验证、无足够证据分别需要什么条件？

*与v3关系：新增：优先补齐JD“设计实验方案”。*


### 4｜指标诊断与验证


#### T4.1｜实现可解释的候选异动检测

**级别：P0｜前置：T1.5｜初始状态：not_started**

**做什么：** 沿用同星期过去4周的中位数与MAD作为启发式基线，明确通常只有3–4个历史点。只使用当前日期之前的数据；补齐日期并区分缺数据与真实零。设置min_history、业务绝对/相对变化阈值与MAD=0分支；scale失效时返回insufficient_history/zero_scale，不加极小数制造巨大z。

**交付物：** `anomaly/detect.py`；`config/anomaly.yaml`；`reports/anomaly_flags.csv`

**验收：** 历史不足、零基线、缺分区测试通过；带输出status与reason；阈值在案例筛选前固定。MAD分数不称为已校准的统计显著性，也不预设某天必须报警。

**你需要解释：** 检测阈值和统计显著性有什么不同？为什么先检查数据完整性？

*与v3关系：修订 v3 T2.2。*


#### T4.2｜保持恒等式的三因子变化拆解

**级别：P0｜前置：T4.1｜初始状态：not_started**

**做什么：** 单独用同一历史日期集合的U、B、V均值构造分解基准，再导出R=B/U、M=V/B。输出各因子的log变化、总log变化和可用时的份额。金额为零、因子非正、质量不合格、总变化过小时返回明确不适用状态；不要强算百分比。

**交付物：** `anomaly/decompose.py`；`tests/test_decompose.py`；`reports/decomposition.csv`

**验收：** 输入有效时log变化相加等于总log变化；份额相加约为1但可以负数或大于1；不适用样例返回状态而不是NaN串；标注这是代数贡献不是因果归因。

**你需要解释：** 检测基线为什么可以不同于分解基准？份额为负说明什么？

*与v3关系：保留 v3 T2.3 的一致基准，补齐零值和符号边界。*


#### T4.3｜维度贡献与分布变化排序

**级别：P0｜前置：T4.2｜初始状态：not_started**

**做什么：** 每个互斥维度独立outer join，未知类别保留，缺失切片填0。输出金额差、带符号贡献、分布份额和JS surprise。方向一致的贡献与抵消项分开呈现，TopK仅用于摘要。0.67为人为摘要覆盖阈值，不视为因果解释概率；不跨维度累加，也不按基数不同的维度直接宣布谁最真实。

**交付物：** `anomaly/attribution.py`；`tests/test_attribution.py`；`reports/dimension_contributions.csv`

**验收：** 新增类别、消失类别、完全无变化、正负抵消、全零分布测试通过；每个维度所有金额差之和与全局差一致；总差近零或分布不能定义时输出状态。代码名与报告均称简化贡献排序，不称论文完整复现。

**你需要解释：** 为什么新增类别要outer join？维度贡献为什么不能再相加？

*与v3关系：修订 v3 T2.4；保留 Adtributor 思路而非扩展论文级算法。*


#### T4.4｜形成一个真实日志观察案例

**级别：P0｜前置：T4.1、T4.2、T4.3、T2.2｜初始状态：not_started**

**做什么：** 依据预设筛选规则选择数据质量合格的候选变化，不预设黑五必有异常。报告依次写变化事实、数据检查、因子贡献、切片、替代解释、后续核查。未检出异常时如实呈现并选择一个“变化但未达告警阈值”的对比案例；不改阈值制造发现。更新业务备忘录。

**交付物：** `reports/case_observed_change.md`；`reports/business_decision_memo.md`

**验收：** 报告数字均能追溯到运行结果；没有“证明由促销/疫情造成”等缺证据表述；没有数据的履约、渠道、库存内容明确标为待核查；候选窗口与因果事故标签分开。

**你需要解释：** 你真正定位到了什么？还不能确定什么？给业务团队的下一步是什么？

*与v3关系：替换 v3 T2.5 固定“黑五窗口”叙事。*


#### T4.5｜人工故障注入与恢复验证

**级别：P1｜前置：T4.4、T1.4｜初始状态：not_started**

**做什么：** 核心只做一个事件级故障：在独立受控样本的某日期减少一个已声明品类的已声明比例事件；重建该测试空间的全部相关指标，避免直接改一张汇总表。另设一个不改数据的空白对照。选择规则和强度事先写入fault_manifest；必要时在抽样完整历史上测试，不重复拷贝全量。

**交付物：** `tests/faults/inject_events.py`；`reports/fault_manifest.json`；`reports/fault_injection.md`

**验收：** 原始文件哈希不变；能列出真实移除的记录数/金额和预期受影响范围；报告是否报警、TopK是否命中及恢复后是否回到基准。未命中算方法局限，不调数据到必胜；单案例不报告泛化准确率。

**你需要解释：** 人工测试和真实事故验证有什么不同？为何要从事件层重建指标？

*与v3关系：替换 v3 T2.6 汇总层修改导致不一致的问题。*


### 5｜实验方法校验


#### T5.1｜构建固定前置期用户队列

**级别：P0｜前置：T1.5｜初始状态：not_started**

**做什么：** 默认前置期2019-10-01至10-14，结果期10-15至10-28，均按UTC闭区间。入组=前置期内有合格事件的用户；以名单为左表关联结果，未返回者仍保留，确认日志完整时未出现的行为为0。只研究既有用户。金额存在未知购买值时保留unknown标记，不把缺失当零；金额实验暂停或使用事先定义的观测金额口径。

**交付物：** `sql/exp/user_window.sql`；`reports/cohort_definition.md`；`warehouse/exp_user_window/`

**验收：** 每用户一行，入组名单只依赖处理前记录；未返回用户保留；窗口不重叠；金额缺失与结构零不同；数值表和用户ID分离保存，选择列及抽样规则可复现。

**你需要解释：** 为何不能默认只保留实验后活跃用户？当前队列能否代表全部新老用户？

*与v3关系：修订 v3 T3.1 的实验期活跃入组。*


#### T5.2｜稳定分组与SRM质量检查

**级别：P0｜前置：T5.1｜初始状态：not_started**

**做什么：** 固定用户标识序列化，采用稳定哈希（如SHA-256截断）结合实验ID映射到[0,1)，按声明的50:50分配。不要使用Python内置hash或将不同实验ID称作保证正交。实现按声明概率的SRM，0.001是项目质量阈值。独立rng负责模拟抽样与效应注入。

**交付物：** `abtest/assign.py`；`abtest/srm.py`；`tests/test_assignment.py`；`reports/assignment_validation.md`

**验收：** 同配置重复和跨实现分组一致；空ID被拒绝；组间关系用统计容差诊断，不能硬要求|r|<0.01适合任意n。自然出现的SRM信号记录而不重新分组“修平”；预置明显失衡构造样例能被发现。

**你需要解释：** 确定性分流和随机化如何兼容？SRM检验的原定比例从哪里来？

*与v3关系：修订 v3 T3.2，避免无条件正交和固定相关阈值。*


#### T5.3｜实现统一统计结果接口

**级别：P0｜前置：T5.2｜初始状态：not_started**

**做什么：** 实现用户级Welch均值差、二元结果近似/区间方法，以及ratio-of-sums的delta method。统一输出n、组均值、差值、SE、CI、p、单位、假设、状态。比率X/Y按用户聚合，单个用户Y=0可以保留，只有整体分母无效时不可计算。使用充分统计量或受控数值数组，不循环收集事实表。

**交付物：** `abtest/stats.py`；`tests/test_stats.py`；`docs/statistical_contract.md`

**验收：** 与独立公式/库在适用构造样本上一致；空组、n不足、常量、零分母有状态；delta method与用户级成对bootstrap在正则样本上按Monte Carlo误差比较，不把任意数据都小于10%误差作为绝对条件。

**你需要解释：** 为什么ratio-of-sums不同于mean-of-ratios？为什么要按用户而非事件做方差？

*与v3关系：修订 v3 T3.3 的接口与验收。*


#### T5.4｜完成基础A/A校准报告

**级别：P0｜前置：T5.3｜初始状态：not_started**

**做什么：** 先少量模拟验证，再按预先记录的配置执行200–300次基准重复；资源受限时使用固定用户样本并写清样本量。首版校验converted和金额（仅在质量允许时），输出逐次记录、误报比例的二项区间、p值分布、失败/SRM状态。p值均匀性检验只是辅助诊断。

**交付物：** `abtest/aa_test.py`；`reports/aa_runs.csv`；`reports/aa_validation.md`

**验收：** 完整保存所有计划重复及失败原因，不挑选“好看”的种子或删除不显著/显著结果；报告Monte Carlo不确定性；不要求恰好5%或KS必须通过；结论仅限固定数据、指标与分流条件，不代表线上端到端系统。

**你需要解释：** 为什么300次并不保证误报率恰好5%？低频或重尾指标有什么风险？

*与v3关系：保留 v3 T3.4，去除结果导向验收；CUPED增强后追加。*


#### T5.5｜已知效应注入与可复现单次评估

**级别：P0｜前置：T5.3｜初始状态：not_started**

**做什么：** 只注入converted，保留原始Y0。以总体基准率p0设delta，令非转化用户以q=delta/(1-p0)得到潜在Y1，使用独立随机源；处理分配前构造完整潜在结果，观测Y按分组选择Y0/Y1。记录目标期望效应与实际有限样本效应。relative_lift先转delta。模拟表独立命名，禁止再用于原始金额/转化恒等式。

**交付物：** `abtest/inject.py`；`tests/test_inject.py`；`reports/effect_injection.md`；`exp/simulations/`

**验收：** delta=0不改变数据；p0=1的正向注入拒绝；范围检查通过；平均注入率按理论Monte Carlo容差核对。保留v3的分组内翻转作小型公式测试，但正式功效评估不依赖分配后p_B来重新定义每轮效应。

**你需要解释：** 相对2%和增加2个百分点有何区别？期望效应与单次实现效应为什么不完全相同？

*与v3关系：保留 v3 修正公式，新增分配前潜在结果以规范功效模拟。*


#### T5.6｜CUPED调整与方差口径校准

**级别：P1｜前置：T5.3、T5.4｜初始状态：not_started**

**做什么：** 沿用合并样本估计共享theta和X中心的首版实现，明确大样本近似并在A/A中验证。金额用pre_amount、converted用pre_converted；只有质量允许且协变量有变异时运行。主报告使用处理效应SE平方比衡量方差变化，而不是把结果变量混合方差当成唯一证据。

**交付物：** `abtest/cuped.py`；`tests/test_cuped.py`；`reports/cuped_validation.md`

**验收：** 明确variance_ratio=SE_adj²/SE_raw²，variance_reduction=1-ratio；允许减少比例为负并解释；常量X返回未调整结果；用共同分组比较调整前后，报告效果/SE/CI/FPR，不要求必然提升。

**你需要解释：** 剩余36%方差和降低36%差在哪？为什么theta按A/B分别居中会出问题？

*与v3关系：修正 v3 T3.3 与附录A的方差缩减命名。*


#### T5.7｜功效曲线及适用条件报告

**级别：P1｜前置：T5.5、T5.6｜初始状态：not_started**

**做什么：** 基于冻结用户样本选择包含0的效应网格；用基准率、n和设计MDE选择几个可能处于20%–80%功效附近的点，此范围只是选点目标。每个点初步100次重复，给二项区间；raw/CUPED使用相同分组和注入结果。必要时做第二个样本规模，不扩成庞大参数搜索。

**交付物：** `abtest/power.py`；`reports/power_runs.csv`；`reports/power_report.md`；`reports/figures/power_curve.png`

**验收：** 报告零效应校准、实现效应、区间和未成功计算次数；点估计可轻微不单调，两条曲线可交叉；不强行修改模拟让CUPED胜出。若功效全饱和，先确认方法再调整预先说明的n/效应范围。

**你需要解释：** 功效为什么受样本量和基准率影响？为什么观察到的功效曲线不必严格单调？

*与v3关系：修订 v3 T3.6。*


### 6｜Hadoop与性能证据


#### T6.1｜独立验证HDFS/YARN环境

**级别：P1｜前置：T1.4、T0.2｜初始状态：not_started**

**做什么：** 按已授权实际环境选择本地或MRC Linux单机伪分布式；确定Hadoop与其JDK、Spark/PySpark的兼容组合并单独锁定。HDFS副本数1仅作练习。初始化与启动拆开：初始化只面向确认的空目录，发现已有NameNode元数据直接拒绝格式化。绑定本机/受限网络，不公开管理UI。

**交付物：** `infra/hadoop/`；`scripts/init_hdfs_once.sh`；`scripts/start_hadoop.sh`；`docs/hadoop_environment.md`

**验收：** 可列出DataNode/NameNode状态、HDFS写读一致；停止/启动不丢文件；无自动重复format；给出完整内存预算，包含Driver、executor/Python、AM及守护进程。失败记录为blocked，不用原理笔记替代成功。

**你需要解释：** NameNode管什么？单副本伪分布式能证明多机容错吗？

*与v3关系：把 v3 T1.1 后移，并保护初始化操作。*


#### T6.2｜通过YARN完成一条真实指标作业

**级别：P1｜前置：T6.1、T1.4｜初始状态：not_started**

**做什么：** 将已验证的受控行为样本上传HDFS，通过spark-submit --master yarn运行实际指标SQL并写回HDFS；先单executor小预算验证，再视资源调整。保存提交参数、application ID、driver/executor位置、日志和返回码。

**交付物：** `scripts/run_metrics_yarn.sh`；`reports/hadoop_practice.md`；`runs/yarn/`

**验收：** 不仅有Pi样例：真实业务输出与本地相同样本结果一致；能查看作业记录与失败日志；README明确单机伪分布式、数据范围及client/cluster模式。仅从HDFS读数据但local计算不能写成Spark on YARN。

**你需要解释：** local、YARN client、YARN cluster三种模式中Driver/Executor在哪里？

*与v3关系：强化 v3 T1.1 的Pi验证，使之成为项目证据。*


#### T6.3｜完成两项受控性能对照

**级别：P1｜前置：T1.5｜初始状态：not_started**

**做什么：** 优先做CSV vs Parquet、无日期分区 vs 日期分区。固定同一输入、口径、查询与结果；测试必须触发相同实际计算和输出，格式转换成本单列。配置实验至少重复3次并报告原始耗时、中位数、范围、冷/热状态、执行计划、扫描/Shuffle/溢写指标（可获取项）。

**交付物：** `benchmarks/`；`reports/performance.md`；`runs/benchmarks.csv`

**验收：** 优化前后结果一致；未通过结果检查的运行不作为性能收益；没有“删除高频session使作业更快”的伪优化，也不把启用默认AQE当收益来源。没有加速可照常交付，解释数据规模和启动开销。

**你需要解释：** 为什么分区可能有帮助，也可能没有？如何区分计算时间、排队时间与数据传输时间？

*与v3关系：新增可核验对照，替代 v3 简历里缺依据的性能表述。*


### 7｜发布与面试


#### T7.1｜生成可追溯README与运行手册

**级别：P0｜前置：T2.3、T3.2、T3.4、T4.4、T5.4、T5.5｜初始状态：not_started**

**做什么：** README按业务问题、三类证据、关键结果、复现步骤、工程环境、边界组织；所有展示数字引用对应run_id。整理最小演示数据（自造小样本）、命令、依赖锁、指标字典、报告清单。只有已完成模块进入成果，其他模块列下一步。

**交付物：** `README.md`；`docs/runbook.md`；`docs/result_registry.csv`；`reports/period_summary.md`

**验收：** 干净环境可按记录复跑自造小样本；结果表、报告、简历数字一致；没有凭空存在的文件或命令；不会因展示而公开下载凭据、未授权原始数据或SSH密钥。

**你需要解释：** 没有原始大文件，评审者怎样验证你的逻辑？哪个run_id支持核心结论？

*与v3关系：重组 v3 T4.3。*


#### T7.2｜简历证据映射与阶段完成审查

**级别：P0｜前置：T7.1｜初始状态：not_started**

**做什么：** 制作“简历表述—结果文件—运行记录—个人解释”四列证据表。实际处理规模、模拟次数、环境、CI和性能均从登记表读取。P0完成可准备投递；只有T6.2完成才能写YARN，只有T5.6完成才能填CUPED，只有E2完成才能写留出模型结果。

**交付物：** `reports/resume_evidence.md`；`reports/project_release_checklist.md`

**验收：** 每条简历经历可验证；无日期驱动的“第5天必可投”；不得使用“搭建线上企业平台/业务增长由我带来”等超出个人项目的表述；未完成能力不通过括号二选一混入简历。

**你需要解释：** 项目在哪些要求上能证明能力，哪些需要实习或协作经历补充？

*与v3关系：修订 v3 T5.2 与固定六天交付承诺。*


#### T7.3｜理解验收与现场修改练习

**级别：P0｜前置：T7.2｜初始状态：not_started**

**做什么：** 准备业务、SQL/Spark、统计、故障四个一分钟说明；实际手写一条分母正确的SQL、解释一个执行计划、修复一个边界测试。保留失败案例及改进记录。纠正v3附录中“Uplift必须随机实验”“随机保证所有样本完全平衡”等绝对表述。

**交付物：** `reports/interview_notes.md`；`reports/learning_log.md`

**验收：** 不照稿能说出指标粒度、处理边界和下一步；能独立更改一个配置/查询并预测影响；知道CATE识别依赖设计或额外假设，不能把模型相关关系直接当因果。

**你需要解释：** 哪些部分由工具辅助？哪些判断是你作出的？遇到负面结果如何解释？

*与v3关系：升级 v3 T5.3 与附录A，不再只背标准答案。*


### 扩展｜Uplift与云迁移


#### E1｜训练响应/S/T三个有限基线

**级别：P2｜前置：T3.1、T3.3、T7.1｜初始状态：not_started**

**做什么：** 只有核心版完成后启动。特征固定f0–f11；S额外含treatment，响应模型不含treatment；禁止exposure、visit等处理后变量作为conversion基线特征。默认不做负标签采样；训练太大时先在train按用户/记录与标签无关抽样，并记录每个组的覆盖。所有选择只用valid。

**交付物：** `uplift/models.py`；`reports/uplift_model_card.md`

**验收：** 特征白名单与数据划分检查通过；常数模型作logloss基准；记录失败/不优于基准的模型，不要求T-learner必胜。模型可加载复现valid预测。

**你需要解释：** 响应预测与增量预测区别是什么？为什么样本少的对照组会增加估计不稳定？

*与v3关系：简化 v3 T6.1–T6.3，首版取消额外负采样支线。*


#### E2｜冻结方案后评价留出排序

**级别：P2｜前置：E1｜初始状态：not_started**

**做什么：** 先用人工小样本固定Qini实现，再一次性执行预定test评估。主指标为未归一化Qini面积（与固定随机基线的差），不使用未定义的“完美曲线”。按固定覆盖比例输出估计增量及分组Bootstrap区间，处理无对照前缀、并列分数和稀疏分组。所有图标明估计对象与公开抽样限制。

**交付物：** `uplift/evaluate.py`；`tests/test_uplift_metrics.py`；`reports/uplift.md`；`reports/uplift_test_metrics.csv`

**验收：** 测试集未参与模型选择；三个模型按相同规则评价；允许曲线不优于随机线；整体估计接近0或为负时不输出误导性的“前20%贡献全量X%”。数据bug修复后的重评估必须登记，不能伪装全新盲测。

**你需要解释：** Qini的纵轴是什么？高分组不显著和模型完全无用是否同一结论？

*与v3关系：修订 v3 T6.4–T6.5 的定义与结果导向验收。*


#### E3｜条件化投放建议与方法迁移说明

**级别：P2｜前置：E2｜初始状态：not_started**

**做什么：** 仅按事先声明的覆盖候选与valid选定规则解释test结果。说明高响应未必高增量；缺少成本和商业约束时不输出真实ROI或最佳预算。向补贴、推荐等场景的迁移只写需要满足的实验和字段条件，不把广告数据重命名成补贴数据。

**交付物：** `reports/uplift_decision_note.md`

**验收：** 建议含不确定性和下一步实验，允许“证据不足，暂不改变策略”；没有确定性“只投前40%”。

**你需要解释：** 为什么公开基准上的排序效果不能直接变成拼多多的预算决策？

*与v3关系：替换 v3 T6.5 的确定性收益措辞。*


#### E4｜有授权时迁移学校云或HPC

**级别：P2｜前置：T1.4、T0.2｜初始状态：not_started**

**做什么：** MRC授权与资源已确认后只新增profile、数据路径与运行手册，不修改业务指标SQL。若使用Spartan，按其调度和软件规则运行，并如实区分Spark local/standalone与Hadoop。先迁移同一核验样本，再扩大日期范围。

**交付物：** `config/mrc.yaml`；`docs/cloud_runbook.md`；`reports/cross_environment_check.csv`

**验收：** 用途与资源获授权，密钥不入仓库；同输入输出一致；记录CPU/RAM/存储和实际运行模式。权限未获批不阻塞本地核心，也不创建收费资源。

**你需要解释：** 为什么同一业务逻辑应跨环境保持一致？单节点多线程为什么不是多机集群？

*与v3关系：新增可选环境分支，不假定MRC当前可用。*

## 6. 推荐执行顺序与今晚的范围

### 6.1 工作轮次

| 轮次 | 主任务 | 完成后才能进入下一步的条件 |
|---|---|---|
| 1 启动 | T0.1–T0.4，T1.1最小样本 | 真实数据能够读写，来源与基本计数可查 |
| 2 可信指标 | T1.2–T1.5 | 主口径一致，跨引擎与重跑通过 |
| 3 业务与实验 | T2.1–T2.3，T3.1–T3.2，T3.4 | 有业务问题及公开实验评估，不只是图表 |
| 4 指标诊断 | T4.1–T4.4；资源允许再T4.5 | 一条可追溯观察案例，不能强造黑五原因 |
| 5 基础方法校验 | T5.1–T5.5 | 分流/统计/基础A/A/已知效应可核对 |
| 6 核心发布 | T7.1–T7.3 | 达G2，仅按已完模块写项目 |
| 7 完整增强 | T3.3、T4.5、T5.6–T5.7、T6.1–T6.3 | 达G3，有实际Hadoop、CUPED和性能证据 |
| 8 可选扩展 | E1–E4按需 | 不影响核心报告稳定，不回看测试集调参 |

这不是必须持续八天的排期，而是工作顺序。复杂任务可拆成多次会话；每次以一个可核验任务结束。云审批、下载和Hadoop兼容问题不阻塞可独立完成的本地样本工作。Criteo的T3.1/T3.2在下载齐全时可提前，不必等REES46所有高级模块。

### 6.2 今晚：只交付一条小闭环

**顺序：T0.1 → T0.2 → T0.3 → T0.4（当前所需文件）→ T1.1的小样本版本。** 同时做最小日指标和独立小样本核对，但不要因此把完整T1.3/T1.4提前标记done。

今晚建议输入为10万–100万行工程样本，输出事件数、活跃用户数、购买用户数、购买事件数、观测购买金额及质量标记。样本不是总体统计依据。全量session分析、去重候选窗口、完整漏斗、Hadoop、A/A 300次、模型和看板都不属于今晚门槛。

今晚的成功条件：①实际环境记录；②文件清单；③可读回的Parquet；④至少一张真实计算的指标表；⑤一次独立核对；⑥运行日志。未完成项保留明确状态，不编造数字。

## 7. 自动化协作与每任务收据

Codex每次只实现一个任务或一个明确子任务。先查看依赖、现有代码与配置，再实施；不得跨任务静默修改口径、数据或验收条件。发生无法解析的源字段时记录阻塞，不自行填造。

每次输出的收据至少包括：

```text
task_id / status / git_commit
actual_environment / config_hash / input_manifest_hash
commands_run / start_time / end_time / elapsed_seconds
input_scope / input_rows / output_rows / output_paths
checks: expected, actual, pass_or_fail
metrics_definition_version / run_id
known_issues / result_limitations / proposed_next_task
```

耗时、CPU、内存等无法测量的字段标记unavailable，不能估计后写成实测。每个报告数字记录对应SQL/函数、输入范围和run_id。代码注释英文，业务解释中文；关键术语保留英文。用户除验收外必须能解释指标、修改关键SQL并讲清一个失败案例。

## 8. 发布标准与简历写法

首版完成P0不要求模型胜出或Hadoop必然已完成。完整目标需P0+P1。未实施的部分不能靠原理笔记“替代经验”。

简历结构建议写三类证据，而不是列满方法：

1. **数据与可靠性：** 实际处理范围、Spark SQL表结构、独立核验和可复跑机制。
2. **业务与诊断：** 实际观察到的变化、贡献分析、核查建议及其证据限制。
3. **实验与验证：** 公开实验评估、离线A/A及已知效应校验；CUPED/Hadoop/模型只在完成后增加。

禁止预填：1.1亿或2.85亿、ETL提升百分比、FPR=5.x%、CUPED固定收益、Qini优于基线、黑五/疫情真实根因、线上GMV增长或企业ROI。数据发布规模与本人实际处理规模分开。

## 9. 风险与退出机制

| 风险 | 触发信号 | 处理 |
|---|---|---|
| 环境安装拖住项目 | 小型Spark尚无法运行 | 暂停Hadoop，收窄到固定Python/Java/Spark环境；保留问题日志 |
| MRC权限/配额不明 | 无有效项目或用途未授权 | 本地继续；不占用共享资源、不上传密钥 |
| 数据过大 | 内存压力或磁盘保护线触发 | 固定用户跨期抽样、分批处理；不行级随机删数据制造规模 |
| 指标不一致 | Spark/DuckDB对不上 | 阻止扩量；查日期、分母、精度、过滤和连接 |
| 真数据没有“漂亮异常” | 检测没有候选或无外部标签 | 如实写未告警案例；人工故障仅作独立验证 |
| A/A或CUPED异常 | FPR或SE超出预期 | 查分组、重尾、缺失与统计假设；保存所有结果 |
| 模型无收益 | 留出不优于基线 | 报告模型与样本限制，不在test反复调参 |
| 工作量超出预期 | 核心报告尚未完成 | 删P2、后移P1；不删P0的数据正确性和实验设计 |

## 10. v3→v4迁移对照

| v3项 | v4位置/变化 |
|---|---|
| T1.1/T1.1b Hadoop优先 | T0.2/T0.3先本地；T6.1/T6.2补真实Hadoop实践 |
| T1.2–T1.4 | T0.4、T1.1/T1.2；增加清单防重、金额/时区/缺失处理 |
| T1.5 | T1.3/T1.4与T2.1；取消默认事件去重，细化漏斗粒度 |
| T2.1 | T2.2；增加阶段总结、T2.3业务备忘录 |
| T2.2–T2.5 | T4.1–T4.4；增加状态、零值、符号和自然案例边界 |
| T2.6 | T4.5事件级故障和空白对照，不直接篡改一张指标表 |
| T3.1–T3.3 | T5.1–T5.3；固定前置队列、稳定分流、统计契约 |
| T3.4–T3.6/CUPED | T5.4–T5.7；工程与研究验收分离，修正方差与功效解释 |
| T4.1/T4.2 | T3.1–T3.3；先split，增加公开基准边界 |
| 原稿缺少完整下一轮设计 | 新增T3.4 |
| T4.3/T5.2/T5.3 | T7.1–T7.3；证据登记、实际完成门槛和理解验收 |
| T6.1–T6.5 | E1–E3延后；取消首版负采样支线，明确Qini与不确定性 |
| 固定6天与第5天可投 | G0–G4和工作轮次，不保证日历节点 |
| 无云资源单一路径 | 配置分离与E4可选迁移，不假定已获资源 |

## 11. 外部方法与软件依据

这些来源用于本版的方法/实现约束，不代表已经下载或实测全部数据。访问核查日期：2026-09-16。原始v3来源是用户提供的 `Pasted markdown(1).md`。

[S1] Apache Spark 3.5.8 Overview：Java/Python支持范围、ARM64与本地模式。https://spark.apache.org/docs/3.5.8/

[S2] PySpark 3.5.8 DataFrame.toPandas：结果集中进入Driver内存。https://spark.apache.org/docs/3.5.8/api/python/reference/pyspark.sql/api/pyspark.sql.DataFrame.toPandas.html

[S3] Spark 3.5.8 Performance Tuning：分区、AQE、连接与优化策略。https://spark.apache.org/docs/3.5.8/sql-performance-tuning.html

[S4] Criteo AI Lab, Criteo Uplift Prediction Dataset：随机广告实验来源、旧版泄漏勘误、修正版参考规模。https://ailab.criteo.com/criteo-uplift-prediction-dataset/ 。更详细的发布抽样限制沿用v3所列边界，实施时应随实际下载包和原论文再次核对，不能仅凭参考规模推断。

[S5] Microsoft Research, p-Values for Your p-Values：离线A/A作为统计可信性诊断及常见失败情形。https://www.microsoft.com/en-us/research/articles/p-values-for-your-p-values-validating-metric-trustworthiness-by-simulated-a-a-tests/

[S6] Microsoft Research, Deep Dive Into Variance Reduction：CUPED与处理效应估计方差。https://www.microsoft.com/en-us/research/articles/deep-dive-into-variance-reduction/

[S7] Microsoft Research, Patterns of Trustworthy Experimentation: Pre-Experiment Stage：假设、指标、随机化单位与实验准备。https://www.microsoft.com/en-us/research/articles/patterns-of-trustworthy-experimentation-pre-experiment-stage/

[S8] scikit-learn, Common pitfalls and recommended practices：测试集隔离与数据泄漏。https://scikit-learn.org/stable/common_pitfalls.html

[S9] Apache Hadoop, Single Node Setup：单机伪分布式与YARN。https://hadoop.apache.org/docs/stable/hadoop-project-dist/hadoop-common/SingleCluster.html

[S10] Spark 3.5.8 Running on YARN：提交模式、环境、日志、安全和资源配置。https://spark.apache.org/docs/3.5.8/running-on-yarn.html

---

**开工原则：先做出能够解释、复现和核验的结果，再扩量、增强和包装。**
