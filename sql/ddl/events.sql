-- Schema documentation only; no Hive catalog is created.
-- All raw strings, including empty strings, are preserved.
-- See docs/event_parsing_contract.md for validity and monetary scope.
CREATE TABLE fact_events (
  event_time STRING,
  event_type STRING,
  product_id STRING,
  category_id STRING,
  category_code STRING,
  brand STRING,
  price STRING,
  user_id STRING,
  user_session STRING,
  event_timestamp_utc TIMESTAMP,
  event_date_utc DATE,
  price_decimal DECIMAL(18,2),
  time_missing BOOLEAN,
  time_invalid BOOLEAN,
  user_id_missing BOOLEAN,
  user_id_invalid BOOLEAN,
  product_id_missing BOOLEAN,
  product_id_invalid BOOLEAN,
  category_id_missing BOOLEAN,
  category_id_invalid BOOLEAN,
  category_code_missing BOOLEAN,
  brand_missing BOOLEAN,
  session_missing BOOLEAN,
  event_type_unknown BOOLEAN,
  price_missing BOOLEAN,
  price_invalid BOOLEAN,
  price_nonfinite BOOLEAN,
  price_negative BOOLEAN,
  price_zero BOOLEAN,
  price_precision_exceeded BOOLEAN,
  price_scale_exceeded BOOLEAN,
  event_eligible BOOLEAN,
  amount_eligible BOOLEAN,
  source_id STRING,
  scope_id STRING,
  input_sha256 STRING,
  contract_version STRING,
  run_id STRING,
  parsed_at_utc TIMESTAMP
) USING PARQUET;
